/*
Este código define uma função que adiciona comportamentos a elementos da página HTML ao carregar a página.
 Os comportamentos incluem alternar a classe "sidebar-toggled" do elemento "body" quando os elementos 
 "#sidebarToggle" ou "#sidebarToggleTop" são clicados, ocultar o elemento ".sidebar .collapse" quando a classe 
 "toggled" é adicionada ao elemento ".sidebar" e aparecer ou ocultar o elemento ".scroll-to-top" quando o usuário 
 rola a página. Além disso, a função também define comportamentos para quando a janela é redimensionada para 
 abaixo de 768px ou 480px de largura, incluindo ocultar o elemento ".sidebar .collapse" e adicionar a classe 
 "toggled" ao elemento ".sidebar". A função também desabilita o rolamento da página quando o usuário rola a barra 
 lateral fixa com o mouse e permite ao usuário rolar até o topo da página quando o elemento ".scroll-to-top" é clicado.
*/
!(function (l) {
  "use strict";
  l("#sidebarToggle, #sidebarToggleTop").on("click", function (e) {
    l("body").toggleClass("sidebar-toggled"),
      l(".sidebar").toggleClass("toggled"),
      l(".sidebar").hasClass("toggled") &&
        l(".sidebar .collapse").collapse("hide");
  }),
    l(window).resize(function () {
      l(window).width() < 768 && l(".sidebar .collapse").collapse("hide"),
        l(window).width() < 480 &&
          !l(".sidebar").hasClass("toggled") &&
          (l("body").addClass("sidebar-toggled"),
          l(".sidebar").addClass("toggled"),
          l(".sidebar .collapse").collapse("hide"));
    }),
    l("body.fixed-nav .sidebar").on(
      "mousewheel DOMMouseScroll wheel",
      function (e) {
        var o;
        768 < l(window).width() &&
          ((o = (o = e.originalEvent).wheelDelta || -o.detail),
          (this.scrollTop += 30 * (o < 0 ? 1 : -1)),
          e.preventDefault());
      }
    ),
    l(document).on("scroll", function () {
      100 < l(this).scrollTop()
        ? l(".scroll-to-top").fadeIn()
        : l(".scroll-to-top").fadeOut();
    }),
    l(document).on("click", "a.scroll-to-top", function (e) {
      var o = l(this);
      l("html, body")
        .stop()
        .animate(
          { scrollTop: l(o.attr("href")).offset().top },
          1e3,
          "easeInOutExpo"
        ),
        e.preventDefault();
    });
})(jQuery);
